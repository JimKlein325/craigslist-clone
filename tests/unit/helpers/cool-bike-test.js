import { coolBike } from 'craigslist-clone/helpers/cool-bike';
import { module, test } from 'qunit';

module('Unit | Helper | cool bike');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = coolBike([42]);
  assert.ok(result);
});
