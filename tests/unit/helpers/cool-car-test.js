import { coolCar } from 'craigslist-clone/helpers/cool-car';
import { module, test } from 'qunit';

module('Unit | Helper | cool car');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = coolCar([42]);
  assert.ok(result);
});
